#pragma once

#include <queue>

using namespace std;

template<class T> class CountingSort 
{
private:
	queue<T> *  _index;

public:
	CountingSort(T *, int);
	~CountingSort(void);
	void Sort(T *, int);

};

template<class T> CoutingSort<T>::CountingSort(T * A, int L)
{
	T
}

template<class T> void CountingSort<T>::Sort(T * A, int L)
{

}